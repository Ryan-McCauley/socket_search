This currently works on macOS and linux. You will need to install nmap as a prerequisite for this script to work.

install ruby 3.1.1

run `bundle install`

run `ruby see_you.rb`
run `ruby inspect.rb`
